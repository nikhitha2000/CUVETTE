var colors = {
    primary : 'red',
    secondary : 'green',
    tertiary : 'yellow',
}

for(color in colors) {
    // console.log(colors[color])
    console.log(color + ' -> ' + colors[color])
}

var colors = ["red", "green", "yellow" ]

for (colour in colors) {
    console.log(colors[colour])
    console.log(colour + ' -> ' +colors[colour])
}
